import React from 'react';

const AddBlockBtn = () => {
  const handleClick = () => {
    // const [blockNumbers, setBlockNumbers] = useState(0);
  };

  return (
    <button onClick={handleClick} type="button">
      點擊新增視窗
    </button>
  );
};

export default AddBlockBtn;
